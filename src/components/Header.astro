---
import { SITE_TITLE } from '../consts';

const pathname = Astro.url.pathname;
const navItems = [
  ['Contents', '/'],
  ['Interludes', '/interludes/'],
];
---

<h1 class="site-title"><a href="/" data-astro-prefetch>{SITE_TITLE}</a></h1>
<nav>
  {navItems.map(([label, href]) => (
    <a
      class:list={['nav-item', { 'nav-item-active': href === pathname }]}
      href={href}
      data-astro-prefetch
    >
      {label}
    </a>
  ))}
</nav>

<style lang="scss">
  @use '../styles/config';

  .site-title {
    grid-area: title;
    font-size: var(--font-xl);
    line-height: 1;
    letter-spacing: 0.2em;

    a {
      text-decoration: none;
      text-transform: uppercase;
      color: var(--color-default);
    }

    @media (--viewport-m) {
      font-size: var(--font-l);
    }
  }

  nav {
    grid-area: nav;
    list-style: none;
    padding-left: 0;
    line-height: 1.2; // tricky line height to match site title
    margin: var(--spacing-half) 0;
    font-size: var(--font);
    user-select: none;

    @media (--viewport-m) {
      font-size: var(--font-l);
      margin: 0;
    }
  }

  .nav-item {
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-right: var(--spacing-half);
    position: relative;
    line-height: 1;
    text-decoration: none;
    color: var(--color-mid);

    &:hover {
      color: var(--color-default);
    }

    &::after {
      content: '\A';
      position: absolute;
      top: calc(50% - var(--font-xxs));
      right: calc(0em - (var(--spacing-half) + var(--font-xxs)) / 2);
      width: var(--font-xxs);
      height: var(--font-xxs);
      border-radius: 50%;
      background: var(--color-accent-default);
      opacity: 0.7;
      display: inline-block;
    }

    &:last-child::after {
      display: none;
    }
  }

  .nav-item-active {
    color: var(--color-default);
  }
</style>
